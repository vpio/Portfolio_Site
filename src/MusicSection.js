import React from 'react';
import Typography from '@material-ui/core/Typography';

const MusicSection = props => {
  return(
    <Typography variant="h2" className="project-title">Music</Typography>
  )
}

export default MusicSection;
